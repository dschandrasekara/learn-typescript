//compile command -  FILENAME=tuple FOLDER=Tuples npm run compile
//output command -  FILENAME=tuple FOLDER=Tuples npm run output


let tuple = [10, "Hello"]; //tuples enable storing multiple fields of different types. Tuples can also be passed as parameters to functions.

tuple.forEach ( function(val) {
    console.log(val);
});
